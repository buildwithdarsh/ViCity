# Project Feature List — ViCity Hotel Booking Platform

> **Generated by**: Deep code analysis of both Backend (BE) and Frontend (FE)
> **Date**: 2026-03-07
> **Codebase**: Next.js App Router (Full-Stack) with Prisma + PostgreSQL

---

## Module 1: Authentication & User System

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 1 | User Registration | Email+password registration with Zod validation. Creates user with hashed password (bcrypt), generates email verification token (24h expiry). Returns user + verification token (dev mode). | `app/api/v1/auth/register/route.ts` (POST), `lib/services/auth.service.ts:register()`, `lib/validators/auth.ts:registerSchema` | `app/(site)/auth/register/page.tsx` — form with name, email, phone (optional), password, confirm password. Calls `POST /api/v1/auth/register`. Stores token in `localStorage("site_token")`. | Complete |
| 2 | User Login | Email+password login. Checks blocked status. Issues JWT access token (15min) + refresh token (7d). Creates session record with IP, userAgent, device. Allows unverified email users to login (verification notice shown in account). | `app/api/v1/auth/login/route.ts` (POST), `lib/services/auth.service.ts:login()` | `app/(site)/auth/login/page.tsx` — form with email, password, show/hide toggle, remember me checkbox. Calls `POST /api/v1/auth/login`. Stores `site_token` + `site_refresh_token` in localStorage. | Complete |
| 3 | Email Verification | GET endpoint with token query param. Validates token, marks `isEmailVerified=true`, deletes used tokens. Resend endpoint for authenticated users. | `app/api/v1/auth/verify-email/route.ts` (GET), `app/api/v1/auth/verify-email/resend/route.ts` (POST), `lib/services/auth.service.ts:verifyEmail()` | `app/(site)/account/page.tsx` — amber verification notice with "Resend" button for unverified users | Complete |
| 4 | Forgot Password | Accepts email, generates password reset token (15min expiry). Returns generic message to prevent email enumeration. Token returned in dev mode. | `app/api/v1/auth/forgot-password/route.ts` (POST), `lib/services/auth.service.ts:forgotPassword()` | `app/(site)/auth/forgot-password/page.tsx` — email input form, shows success state with "Check Your Email" message | Complete |
| 5 | Reset Password | Validates reset token, hashes new password, invalidates all user sessions for security, deletes used tokens. | `app/api/v1/auth/reset-password/route.ts` (POST), `lib/services/auth.service.ts:resetPassword()` | `app/(site)/auth/reset-password/page.tsx` — form with new password + confirm, reads token from URL query param | Complete |
| 6 | Get Profile (Me) | Authenticated GET returns sanitized user profile (excludes password). | `app/api/v1/auth/me/route.ts` (GET), `lib/services/auth.service.ts:getProfile()` | Used in `app/(site)/hooks/useAuth.tsx:fetchMe()`, `app/(site)/account/page.tsx`, `app/(site)/booking/page.tsx` | Complete |
| 7 | Update Profile | Authenticated PATCH updates user fields (name, phone, address, country, profileImage). | `app/api/v1/auth/me/route.ts` (PATCH), `lib/services/auth.service.ts:updateProfile()` | `app/(site)/account/profile/page.tsx` — edit name, phone. Calls `PATCH /api/v1/auth/me`. | Complete |
| 8 | Refresh Token | Accepts refresh token, verifies JWT, rotates to new access+refresh tokens, updates session `lastActive`. | `app/api/v1/auth/refresh-token/route.ts` (POST), `lib/services/auth.service.ts:refreshAccessToken()` | `app/(site)/hooks/useAuth.tsx` — `apiFetch()` intercepts 401 responses, calls `tryRefreshToken()` to rotate tokens, then retries the original request | Complete |
| 9 | Logout | Deletes session by refresh token. | `app/api/v1/auth/logout/route.ts` (POST), `lib/services/auth.service.ts:logout()` | `app/(site)/hooks/useAuth.tsx` — `logout()` calls `POST /api/v1/auth/logout` with refresh token, then clears both `site_token` and `site_refresh_token` from localStorage | Complete |
| 10 | Logout All Devices | Authenticated. Deletes all sessions for the user. | `app/api/v1/auth/logout-all/route.ts` (POST), `lib/services/auth.service.ts:logoutAll()` | No FE UI for "logout all devices" | BE Only |
| 11 | Guest Token | Issues a temporary guest session token with `guest_` prefix userId. No DB session created. | `app/api/v1/auth/guest-token/route.ts` (POST), `lib/services/auth.service.ts:issueGuestToken()` | `app/(site)/booking/page.tsx` — `getGuestSessionId()` calls `POST /api/v1/auth/guest-token` for unauthenticated users | Complete |
| 12 | Auto-Login from Booking | After guest booking+payment, finds or creates user account from booking email. Links booking to user. Auto-verifies email. Returns access token. | `app/api/v1/auth/auto-login/route.ts` (POST), `lib/services/auth.service.ts:autoLoginFromBooking()` | `app/(site)/booking/page.tsx` — called after successful Razorpay payment if user not logged in. Stores returned token. | Complete |
| 13 | Auth Context (FE) | Client-side auth context with login, register, logout, updateProfile, auto-fetch /me on mount. | N/A | `app/(site)/hooks/useAuth.tsx` — `AuthProvider` + `useAuth()` hook, `apiFetch` helper | FE Only |
| 14 | JWT Authentication Middleware | Extracts Bearer token, verifies JWT, returns `{userId, role}`. Supports optional auth variant. | `lib/middleware/auth.ts:authenticate()`, `authenticateOptional()` | N/A | BE Only |
| 15 | Role-Based Authorization | `requireRole(...roles)` middleware checks user role against allowed roles (guest, user, admin, super_admin). | `lib/middleware/auth.ts:requireRole()` | N/A | BE Only |

---

## Module 2: Room & Property Management

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 16 | List Room Types | Paginated list with optional status/search filters. Includes amenities and room unit count. Public access. | `app/api/v1/room-types/route.ts` (GET), `lib/services/room-type.service.ts:listRoomTypes()` | `app/(site)/rooms/page.tsx` (SSR), `app/(site)/page.tsx` (home page rooms section) | Complete |
| 17 | Get Room Type by ID | Returns room type with amenities and unit count. | `app/api/v1/room-types/[id]/route.ts` (GET), `lib/services/room-type.service.ts:getRoomType()` | `app/(site)/rooms/[id]/RoomDetailClient.tsx` | Complete |
| 18 | Create Room Type | Admin only. Creates room type with name, description, basePrice, maxGuests, bedType, roomSize, images, status. | `app/api/v1/room-types/route.ts` (POST), `lib/services/room-type.service.ts:createRoomType()` | `app/admin/rooms/types/page.tsx` — create form in admin | Complete |
| 19 | Update Room Type | Admin only. Partial update of room type fields. | `app/api/v1/room-types/[id]/route.ts` (PATCH), `lib/services/room-type.service.ts:updateRoomType()` | `app/admin/rooms/types/page.tsx` — edit form in admin | Complete |
| 20 | Delete Room Type | Admin only. Blocked if active bookings exist. | `app/api/v1/room-types/[id]/route.ts` (DELETE), `lib/services/room-type.service.ts:deleteRoomType()` | `app/admin/rooms/types/page.tsx` — delete with confirmation | Complete |
| 21 | Room Type Image Upload | Add images to a room type. Appends to existing images array. | `app/api/v1/room-types/[id]/images/route.ts` (POST), `lib/services/room-type.service.ts:addRoomTypeImages()` | `app/admin/rooms/types/page.tsx` | Complete |
| 22 | Room Type Image Delete | Remove specific image from room type's images array. | `app/api/v1/room-types/[id]/images/[imageId]/route.ts` (DELETE), `lib/services/room-type.service.ts:removeRoomTypeImage()` | `app/admin/rooms/types/page.tsx` | Complete |
| 23 | Assign Amenities to Room Type | Replace all amenity assignments for a room type. Deletes existing, creates new. | `app/api/v1/room-types/[id]/amenities/route.ts` (POST), `lib/services/room-type.service.ts:assignAmenities()` | `app/admin/rooms/types/page.tsx` | Complete |
| 24 | List Room Units | Paginated list with status/roomTypeId/floor/search filters. Includes room type info. | `app/api/v1/rooms/route.ts` (GET), `lib/services/room-unit.service.ts:listRoomUnits()` | `app/admin/rooms/units/page.tsx` | Complete |
| 25 | Get Room Unit | Returns room unit with room type. | `app/api/v1/rooms/[id]/route.ts` (GET), `lib/services/room-unit.service.ts:getRoomUnit()` | `app/admin/rooms/units/page.tsx` | Complete |
| 26 | Create Room Unit | Admin only. Creates physical room with roomNumber (unique), floor, roomTypeId, status. | `app/api/v1/rooms/route.ts` (POST), `lib/services/room-unit.service.ts:createRoomUnit()` | `app/admin/rooms/units/page.tsx` | Complete |
| 27 | Update Room Unit | Admin only. Partial update. Validates unique roomNumber. | `app/api/v1/rooms/[id]/route.ts` (PATCH), `lib/services/room-unit.service.ts:updateRoomUnit()` | `app/admin/rooms/units/page.tsx` | Complete |
| 28 | Delete Room Unit | Admin only. Blocked if active bookings exist. | `app/api/v1/rooms/[id]/route.ts` (DELETE), `lib/services/room-unit.service.ts:deleteRoomUnit()` | `app/admin/rooms/units/page.tsx` | Complete |
| 29 | Update Housekeeping Status | Admin only. Updates housekeeping status (clean/dirty/in_progress/inspected). | `app/api/v1/rooms/[id]/housekeeping/route.ts` (PATCH), `lib/services/room-unit.service.ts:updateHousekeepingStatus()` | `app/admin/rooms/units/page.tsx` | Complete |
| 30 | Admin Rooms Hub Page | Landing page for room management with links to types and units sub-pages. | N/A | `app/admin/rooms/page.tsx` | FE Only |

---

## Module 3: Amenities Management

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 31 | List Amenities | Paginated list with category/isActive filters. | `app/api/v1/amenities/route.ts` (GET), `lib/services/amenity.service.ts:listAmenities()` | `app/admin/amenities/page.tsx` | Complete |
| 32 | Get Amenity | Returns single amenity by ID. | `app/api/v1/amenities/[id]/route.ts` (GET), `lib/services/amenity.service.ts:getAmenity()` | `app/admin/amenities/page.tsx` | Complete |
| 33 | Create Amenity | Admin only. Unique name check. Fields: name, icon, category, isActive. | `app/api/v1/amenities/route.ts` (POST), `lib/services/amenity.service.ts:createAmenity()` | `app/admin/amenities/page.tsx` | Complete |
| 34 | Update Amenity | Admin only. Validates unique name. | `app/api/v1/amenities/[id]/route.ts` (PATCH), `lib/services/amenity.service.ts:updateAmenity()` | `app/admin/amenities/page.tsx` | Complete |
| 35 | Delete Amenity | Admin only. Removes room type associations first, then deletes. | `app/api/v1/amenities/[id]/route.ts` (DELETE), `lib/services/amenity.service.ts:deleteAmenity()` | `app/admin/amenities/page.tsx` | Complete |

---

## Module 4: Pricing System

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 36 | List Pricing Rules | Paginated list with roomTypeId filter. Ordered by priority desc. | `app/api/v1/pricing/route.ts` (GET), `lib/services/pricing.service.ts:listPricingRules()` | `app/admin/pricing/page.tsx` | Complete |
| 37 | Get Pricing Rule | Returns single rule with room type. | `app/api/v1/pricing/[id]/route.ts` (GET), `lib/services/pricing.service.ts:getPricingRule()` | `app/admin/pricing/page.tsx` | Complete |
| 38 | Create Pricing Rule | Admin only. Types: base/weekend/seasonal/festival/discount. Fields: roomTypeId, type, price, startDate, endDate, daysOfWeek, minStay, maxStay, priority, isActive. | `app/api/v1/pricing/route.ts` (POST), `lib/services/pricing.service.ts:createPricingRule()` | `app/admin/pricing/page.tsx` | Complete |
| 39 | Update Pricing Rule | Admin only. Partial update. | `app/api/v1/pricing/[id]/route.ts` (PATCH), `lib/services/pricing.service.ts:updatePricingRule()` | `app/admin/pricing/page.tsx` | Complete |
| 40 | Delete Pricing Rule | Admin only. | `app/api/v1/pricing/[id]/route.ts` (DELETE), `lib/services/pricing.service.ts:deletePricingRule()` | `app/admin/pricing/page.tsx` | Complete |
| 41 | Resolve Price | Calculates effective per-night price for a room type over a date range. Priority: festival > seasonal > weekend > base. Returns per-night breakdown + total. | `app/api/v1/pricing/resolve/route.ts` (GET), `lib/services/pricing.service.ts:resolvePrice()` | Used internally during booking creation and search | Complete |

---

## Module 5: Availability & Inventory Management

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 42 | Check Availability | Returns inventory records for a roomTypeId + date range. Auto-creates missing inventory records. | `app/api/v1/availability/route.ts` (GET), `lib/services/availability.service.ts:getAvailability()` | Used during booking flow | Complete |
| 43 | Availability Calendar | Returns availability for ALL active room types over a date range, grouped by room type. | `app/api/v1/availability/calendar/route.ts` (GET), `lib/services/availability.service.ts:getAvailabilityCalendar()` | `app/(site)/booking/page.tsx` — fetches blocked dates for DateRangePicker | Complete |
| 44 | Create Inventory Hold | Locks inventory for checkout (10min). Uses serializable transaction to prevent double-booking. Decrements availableUnits, increments heldUnits. | `app/api/v1/availability/hold/route.ts` (POST+GET), `lib/services/availability.service.ts:createHold()` | Created automatically during `handleBookAndPay()` in booking page | Complete |
| 45 | Get Active Hold by Session | Returns active (non-expired, non-released) hold for a guest session. | `app/api/v1/availability/hold/route.ts` (GET), `lib/services/availability.service.ts:getActiveHoldBySession()` | `app/(site)/booking/page.tsx` — checks for active hold on mount (survives refresh) | Complete |
| 46 | Release Hold | Restores inventory (decrements heldUnits, increments availableUnits). Marks hold as released. | `app/api/v1/availability/hold/[holdId]/route.ts` (DELETE), `lib/services/availability.service.ts:releaseHold()` | No direct FE call; happens on booking cancel or hold expiry | BE Only |
| 47 | Confirm Hold | Converts held units to booked units. Marks hold as released. | `lib/services/availability.service.ts:confirmHold()` | Called internally during payment verification | BE Only |
| 48 | Block/Unblock Dates | Admin only. Sets `isBlocked` flag on inventory records for a date range. | `app/api/v1/availability/block/route.ts` (PATCH), `lib/services/availability.service.ts:blockDates()` | `app/admin/availability/page.tsx` | Complete |
| 49 | Admin Availability Page | Admin UI for viewing and managing availability calendar and blocking dates. | N/A | `app/admin/availability/page.tsx` | FE Only |
| 50 | Auto-Populate Inventory | Inventory records auto-created when availability is queried. Updates totalUnits if room count changes. | `lib/services/availability.service.ts:ensureInventory()` | N/A | BE Only |

---

## Module 6: Booking System

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 51 | Create Booking | Creates booking with guest details, resolves pricing, validates and applies coupon if provided, creates inventory hold, generates booking reference (e.g. "VEL-XXXXXX"), creates BookingRoom entries. Auto-resolves room type if not provided. | `app/api/v1/bookings/route.ts` (POST), `lib/services/booking.service.ts:createBooking()` | `app/(site)/booking/page.tsx` — full booking form with dates, guests, details, coupon input, payment type selection | Complete |
| 52 | List All Bookings (Admin) | Paginated with status/dateFrom/dateTo/search/roomTypeId filters. Includes room type, booking rooms with units. | `app/api/v1/bookings/route.ts` (GET), `lib/services/booking.service.ts:listBookings()` | `app/admin/bookings/page.tsx` — DataTable with filters, search, pagination | Complete |
| 53 | Get Booking by ID | Returns full booking with room type, booking rooms, payments, user info. | `app/api/v1/bookings/[id]/route.ts` (GET), `lib/services/booking.service.ts:getBooking()` | `app/admin/bookings/[id]/page.tsx` — booking detail page | Complete |
| 54 | Update Booking | Modifies notes and guest details. Blocked for cancelled/refunded/checked_out bookings. | `app/api/v1/bookings/[id]/route.ts` (PATCH), `lib/services/booking.service.ts:updateBooking()` | `app/admin/bookings/[id]/page.tsx` | Complete |
| 55 | Cancel Booking | Releases inventory holds, decrements bookedUnits for confirmed bookings, sets status to cancelled with reason and timestamp. | `app/api/v1/bookings/[id]/cancel/route.ts` (POST), `lib/services/booking.service.ts:cancelBooking()` | `app/(site)/account/bookings/page.tsx` — cancel button for pending/confirmed/payment_pending bookings. `app/admin/bookings/[id]/page.tsx` | Complete |
| 56 | Check-In | Admin only. Changes status from confirmed to checked_in with timestamp. | `app/api/v1/bookings/[id]/check-in/route.ts` (POST), `lib/services/booking.service.ts:checkIn()` | `app/admin/bookings/[id]/page.tsx` | Complete |
| 57 | Check-Out | Admin only. Changes status from checked_in to checked_out. Releases booked inventory. | `app/api/v1/bookings/[id]/check-out/route.ts` (POST), `lib/services/booking.service.ts:checkOut()` | `app/admin/bookings/[id]/page.tsx` | Complete |
| 58 | Assign Room Units | Admin only. Assigns specific physical rooms to booking. Validates room availability. Marks assigned rooms as occupied. | `lib/services/booking.service.ts:assignRooms()` | `app/admin/bookings/[id]/page.tsx` | Complete |
| 59 | My Bookings | Authenticated user's own bookings list, filtered by userId. | `app/api/v1/bookings/my/route.ts` (GET), `lib/services/booking.service.ts:listBookings()` with userId filter | `app/(site)/account/bookings/page.tsx` — list with status badges and cancel buttons | Complete |
| 60 | Lookup by Reference | Public. Finds booking by human-readable reference code. | `app/api/v1/bookings/reference/[ref]/route.ts` (GET), `lib/services/booking.service.ts:getBookingByReference()` | `app/(site)/booking/lookup/page.tsx` — search form with reference input | Complete |
| 61 | Confirm Booking Payment | Internal. Converts hold to booked, sets booking status to confirmed + paid. | `lib/services/booking.service.ts:confirmBookingPayment()` | Called internally after payment verification | BE Only |
| 62 | ~~Multi-Room Booking~~ | Removed. Single-villa property — multi-room booking is not applicable. `isMultiRoom` field removed from Prisma schema and booking validator. | — | — | Removed |
| 63 | Booking Confirmation Page | Post-payment confirmation showing booking reference, partial payment notice, auto-redirect to account. | N/A | `app/(site)/booking/confirmation/page.tsx` — shows ref, payment type notice, redirect timer | FE Only |
| 64 | Hold Countdown Timer | Visual countdown showing remaining hold time during checkout. Auto-closes Razorpay on expiry. | N/A | `app/(site)/booking/page.tsx:CountdownTimer` component | FE Only |
| 65 | Blocked Date Detection | On booking page mount, fetches availability calendar. If selected dates are blocked, auto-suggests next available window of same length. | N/A | `app/(site)/booking/page.tsx` — `fetchBlockedDates()` + auto-redirect logic | FE Only |
| 66 | Unavailability Modal | Shows "Oops, you just missed it!" modal when selected dates become unavailable during booking flow. | N/A | `app/(site)/booking/page.tsx` — `showUnavailableModal` state | FE Only |

---

## Module 7: Payment Integration (Razorpay)

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 67 | Create Payment Order | Creates Razorpay order. Supports full/partial (50% advance) payment. Handles balance payments for partially paid bookings. Returns orderId, amount, razorpayKeyId. | `app/api/v1/payments/create-order/route.ts` (POST), `lib/services/payment.service.ts:createPaymentOrder()` | `app/(site)/booking/page.tsx` — calls after booking creation | Complete |
| 68 | Verify Payment | Verifies Razorpay signature (HMAC SHA256). Updates payment status. Handles full and partial payments. Confirms booking on full payment. | `app/api/v1/payments/verify/route.ts` (POST), `lib/services/payment.service.ts:verifyPayment()` | `app/(site)/booking/page.tsx` — Razorpay `handler` callback | Complete |
| 69 | Webhook Handler | Verifies Razorpay webhook signature. Handles `payment.captured` and `payment.failed` events. Idempotent (deduplicates by eventType). Logs all webhook events. | `app/api/v1/payments/webhook/route.ts` (POST), `lib/services/payment.service.ts:handleWebhook()` | N/A (server-to-server) | BE Only |
| 70 | Initiate Refund | Admin only. Calls Razorpay refund API. Updates payment + booking status to refunded. Supports partial refund amount. | `app/api/v1/payments/[id]/refund/route.ts` (POST), `lib/services/payment.service.ts:initiateRefund()` | `app/admin/payments/[id]/page.tsx` | Complete |
| 71 | List All Payments | Admin only. Paginated with status/date filters. Includes booking info and webhook events. | `app/api/v1/payments/route.ts` (GET), `lib/services/payment.service.ts:listPayments()` | `app/admin/payments/page.tsx` | Complete |
| 72 | Get Payment Detail | Returns payment with booking and webhook events. | `app/api/v1/payments/[id]/route.ts` (GET), `lib/services/payment.service.ts:getPayment()` | `app/admin/payments/[id]/page.tsx` | Complete |
| 73 | Get Payments by Booking | Returns all payments for a booking (for multi-payment scenarios). | `app/api/v1/payments/booking/[bookingId]/route.ts` (GET), `lib/services/payment.service.ts:getPaymentByBooking()` | `app/admin/bookings/[id]/page.tsx` | Complete |
| 74 | Partial Payment (50% Advance) | Guest can pay 50% upfront, balance due before check-in. Booking confirmed on first payment. | `lib/services/payment.service.ts:createPaymentOrder()` — `paymentType: "partial"` | `app/(site)/booking/page.tsx` — radio toggle for "Pay in Full" vs "50% Advance" | Complete |
| 75 | Razorpay Checkout Integration | Loads Razorpay JS SDK, opens checkout modal, handles success/dismiss/error callbacks. | N/A | `app/(site)/booking/page.tsx` — dynamic script loading, `Razorpay` constructor, themed modal | FE Only |

---

## Module 8: Coupon & Discount System

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 76 | Create Coupon | Admin only. Unique code (uppercased). Types: percentage/flat. Fields: maxDiscountAmount, minBookingValue, expiryDate, usageLimit, perUserLimit, applicableRoomTypes. | `app/api/v1/coupons/route.ts` (POST), `lib/services/coupon.service.ts:createCoupon()` | `app/admin/coupons/page.tsx` | Complete |
| 77 | List Coupons | Admin only. Paginated with isActive filter. | `app/api/v1/coupons/route.ts` (GET), `lib/services/coupon.service.ts:listCoupons()` | `app/admin/coupons/page.tsx` | Complete |
| 78 | Get Coupon | Admin only. | `app/api/v1/coupons/[id]/route.ts` (GET), `lib/services/coupon.service.ts:getCoupon()` | `app/admin/coupons/page.tsx` | Complete |
| 79 | Update Coupon | Admin only. Partial update. | `app/api/v1/coupons/[id]/route.ts` (PATCH), `lib/services/coupon.service.ts:updateCoupon()` | `app/admin/coupons/page.tsx` | Complete |
| 80 | Delete Coupon | Admin only. Blocked if coupon has been used. | `app/api/v1/coupons/[id]/route.ts` (DELETE), `lib/services/coupon.service.ts:deleteCoupon()` | `app/admin/coupons/page.tsx` | Complete |
| 81 | Validate Coupon | Public. Validates code, checks expiry, usage limits, per-user limits, min booking value, applicable room types. roomTypeId is optional. Returns discount preview. | `app/api/v1/coupons/validate/route.ts` (POST), `lib/services/coupon.service.ts:validateCoupon()` | `app/(site)/booking/page.tsx` — coupon input field with "Apply" button, shows discount amount or error | Complete |
| 82 | Apply Coupon | Internal. Records coupon usage and increments usage count. Called during booking creation when coupon is provided. | `lib/services/coupon.service.ts:applyCoupon()`, called from `lib/services/booking.service.ts:createBooking()` | Coupon code sent in booking creation body when applied | Complete |

---

## Module 9: Notifications System

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 83 | Create In-App Notification | Creates notification record + logs to NotificationLog with channel="in_app". | `lib/services/notification.service.ts:createNotification()` | N/A (used internally) | BE Only |
| 84 | List User Notifications | Authenticated. Paginated, ordered by createdAt desc. | `app/api/v1/notifications/route.ts` (GET), `lib/services/notification.service.ts:listNotifications()` | `app/(site)/account/notifications/page.tsx` — lists notifications with read/unread state | Complete |
| 85 | Mark Notification Read | Authenticated. Validates ownership. | `app/api/v1/notifications/[id]/read/route.ts` (PATCH), `lib/services/notification.service.ts:markAsRead()` | `app/(site)/account/notifications/page.tsx` — click to mark read | Complete |
| 86 | Mark All Read | Authenticated. Marks all unread notifications as read. | `app/api/v1/notifications/read-all/route.ts` (PATCH), `lib/services/notification.service.ts:markAllAsRead()` | `app/(site)/account/notifications/page.tsx` — "Mark all read" button | Complete |
| 87 | Delete Notification | Authenticated. Validates ownership. | `app/api/v1/notifications/[id]/route.ts` (DELETE), `lib/services/notification.service.ts:deleteNotification()` | No delete button in FE | BE Only |
| 88 | Admin Notification Logs | Admin only. Paginated list of all notification logs with channel/type/status/date filters. | `app/api/v1/admin/notification-logs/route.ts` (GET), `lib/services/notification.service.ts:listNotificationLogs()` | `app/admin/notifications/page.tsx` | Complete |
| 89 | Get Notification Log Detail | Admin only. | `app/api/v1/admin/notification-logs/[id]/route.ts` (GET), `lib/services/notification.service.ts:getNotificationLog()` | `app/admin/notifications/page.tsx` | Complete |

---

## ~~Module 10: Search & Filtering~~ (Removed)

> Search module removed — single-villa property does not need room search. `search.service.ts` and `GET /api/v1/search/rooms` route deleted. BookingSearch component on home page links directly to `/booking`.

---

## Module 11: Reviews & Ratings

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 91 | Submit Review | Authenticated. Only for checked_out bookings. One review per booking. Fields: bookingId, rating (1-5), title, body. Status starts as "pending". | `app/api/v1/reviews/route.ts` (POST), `lib/services/review.service.ts:createReview()` | `app/(site)/account/reviews/page.tsx` — "Write a Review" form with booking selector, star rating picker, title, body. Only shows for eligible (checked_out, not yet reviewed) bookings | Complete |
| 92 | List Public Reviews | Public. Only approved reviews. Optional roomTypeId filter. Paginated. | `app/api/v1/reviews/route.ts` (GET), `lib/services/review.service.ts:listPublicReviews()` | `app/(site)/reviews/page.tsx` (SSR), `app/(site)/page.tsx` (home reviews section) | Complete |
| 93 | My Reviews | Authenticated user's own reviews. | `app/api/v1/reviews/my/route.ts` (GET), `lib/services/review.service.ts:listMyReviews()` | `app/(site)/account/reviews/page.tsx` — list with ratings and status badges | Complete |
| 94 | Admin List All Reviews | Admin only. Optional status filter. Includes user, room type, booking reference. | `app/api/v1/admin/reviews/route.ts` (GET), `lib/services/review.service.ts:listAllReviews()` | `app/admin/reviews/page.tsx` | Complete |
| 95 | Approve Review | Admin only. Sets status to "approved". | `app/api/v1/admin/reviews/[id]/approve/route.ts` (PATCH), `lib/services/review.service.ts:moderateReview("approve")` | `app/admin/reviews/page.tsx` | Complete |
| 96 | Reject Review | Admin only. Sets status to "rejected" with optional adminNote. | `app/api/v1/admin/reviews/[id]/reject/route.ts` (PATCH), `lib/services/review.service.ts:moderateReview("reject")` | `app/admin/reviews/page.tsx` | Complete |
| 97 | Hide Review | Admin only. Sets status to "hidden". | `app/api/v1/admin/reviews/[id]/hide/route.ts` (PATCH), `lib/services/review.service.ts:moderateReview("hide")` | `app/admin/reviews/page.tsx` | Complete |
| 98 | Delete Review | Admin only. Permanently removes review. | `app/api/v1/admin/reviews/[id]/route.ts` (DELETE), `lib/services/review.service.ts:deleteReview()` | `app/admin/reviews/page.tsx` | Complete |

---

## Module 12: Admin Dashboard

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 99 | Dashboard Overview | Returns: bookings today/month, revenue today/month, occupancy rate, avg booking value, pending check-ins/outs, recent 10 bookings. | `app/api/v1/admin/dashboard/overview/route.ts` (GET), `lib/services/admin.service.ts:getDashboardOverview()` | `app/admin/dashboard/page.tsx` — StatsCards row (6 cards) + recent bookings table | Complete |
| 100 | Bookings Chart | Returns booking counts grouped by daily/weekly/monthly period. | `app/api/v1/admin/dashboard/bookings-chart/route.ts` (GET), `lib/services/admin.service.ts:getBookingsChart()` | `app/admin/dashboard/page.tsx` — BarChart (recharts) | Complete |
| 101 | Revenue Chart | Returns revenue grouped by daily/monthly for a given year. | `app/api/v1/admin/dashboard/revenue-chart/route.ts` (GET), `lib/services/admin.service.ts:getRevenueChart()` | `app/admin/dashboard/page.tsx` — BarChart (recharts) | Complete |
| 102 | Occupancy Chart | Returns occupancy percentage per date for a date range. | `app/api/v1/admin/dashboard/occupancy-chart/route.ts` (GET), `lib/services/admin.service.ts:getOccupancyChart()` | `app/admin/dashboard/page.tsx` — LineChart (recharts) | Complete |
| 103 | Room Type Breakdown | Returns room types with room count, booking count, base price. | `app/api/v1/admin/dashboard/room-type-breakdown/route.ts` (GET), `lib/services/admin.service.ts:getRoomTypeBreakdown()` | `app/admin/dashboard/page.tsx` — PieChart (recharts) showing booking distribution by room type with percentage labels | Complete |

---

## Module 13: Admin User Management

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 104 | List Users | Admin only. Paginated with search/role filters. Returns user with booking count. | `app/api/v1/admin/users/route.ts` (GET), `lib/services/admin.service.ts:listUsers()` | `app/admin/users/page.tsx` | Complete |
| 105 | Get User Detail | Admin only. Returns user with booking history (last 20) and counts. | `app/api/v1/admin/users/[id]/route.ts` (GET), `lib/services/admin.service.ts:getUser()` | `app/admin/users/[id]/page.tsx` | Complete |
| 106 | Block User | Admin only. Sets isBlocked=true. | `app/api/v1/admin/users/[id]/block/route.ts` (PATCH), `lib/services/admin.service.ts:blockUser()` | `app/admin/users/page.tsx` or `app/admin/users/[id]/page.tsx` | Complete |
| 107 | Unblock User | Admin only. Sets isBlocked=false. | `app/api/v1/admin/users/[id]/unblock/route.ts` (PATCH), `lib/services/admin.service.ts:unblockUser()` | `app/admin/users/page.tsx` or `app/admin/users/[id]/page.tsx` | Complete |
| 108 | Change User Role | Admin only. Changes user role. | `app/api/v1/admin/users/[id]/role/route.ts` (PATCH), `lib/services/admin.service.ts:changeUserRole()` | `app/admin/users/[id]/page.tsx` | Complete |

---

## Module 14: Reports

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 109 | Booking Report | Admin only. Date range. Returns bookings list, status breakdown, total count. | `app/api/v1/admin/reports/bookings/route.ts` (GET), `lib/services/report.service.ts:getBookingReport()` | `app/admin/reports/page.tsx` | Complete |
| 110 | Revenue Report | Admin only. Date range. Returns total revenue, payment method breakdown, daily revenue, transaction list. | `app/api/v1/admin/reports/revenue/route.ts` (GET), `lib/services/report.service.ts:getRevenueReport()` | `app/admin/reports/page.tsx` | Complete |
| 111 | Occupancy Report | Admin only. Date range. Returns per-room-type occupancy rate, peak dates, total booked nights. | `app/api/v1/admin/reports/occupancy/route.ts` (GET), `lib/services/report.service.ts:getOccupancyReport()` | `app/admin/reports/page.tsx` | Complete |
| 112 | Cancellation Report | Admin only. Date range. Returns cancellations list, reason breakdown, total refunds. | `app/api/v1/admin/reports/cancellations/route.ts` (GET), `lib/services/report.service.ts:getCancellationReport()` | `app/admin/reports/page.tsx` | Complete |

---

## Module 15: Audit Logs

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 113 | List Audit Logs | Admin only. Paginated with actorId/action/resourceType/date filters. Includes actor info. | `app/api/v1/admin/audit-logs/route.ts` (GET), `lib/services/admin.service.ts:listAuditLogs()` | `app/admin/audit-logs/page.tsx` — DataTable with search, action/resourceType filters, pagination. Added to admin sidebar. | Complete |
| 114 | Get Audit Log Detail | Admin only. Returns single audit log with actor info. | `app/api/v1/admin/audit-logs/[id]/route.ts` (GET), `lib/services/admin.service.ts:getAuditLog()` | `app/admin/audit-logs/page.tsx` — detail modal on row click | Complete |
| 115 | Create Audit Log | Internal function for recording admin actions. | `lib/services/admin.service.ts:createAuditLog()` | N/A | BE Only |

---

## Module 16: System Settings

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 116 | Get System Settings | Admin only. Returns settings (checkInTime, checkOutTime, taxRate, taxLabel, cancellationPolicy, bookingHoldMinutes, advancePaymentPercent, currency, currencySymbol, timezone, hotelName, hotelAddress, hotelPhone, hotelEmail). Auto-creates default if none exist. | `app/api/v1/admin/settings/route.ts` (GET), `lib/services/admin.service.ts:getSettings()` | `app/admin/settings/page.tsx` | Complete |
| 117 | Update System Settings | Admin only. Upserts settings. Invalidates server-side config cache on save. | `app/api/v1/admin/settings/route.ts` (PATCH), `lib/services/admin.service.ts:updateSettings()`, `lib/config.ts:invalidateConfigCache()` | `app/admin/settings/page.tsx` | Complete |
| 118 | Centralized Pricing Config | All prices, taxes, fees, and charges are driven by SystemSettings — zero hardcoded values in BE or FE. Config loaded server-side via `getSiteConfig()` with 1-min cache. Fields: `taxRate` (%), `taxLabel` (e.g. GST), `advancePaymentPercent` (partial payment %), `currencySymbol`, `bookingHoldMinutes`. Backend services (booking, payment, availability) and all frontend pages (home, booking, rooms, confirmation) consume config dynamically. | `lib/config.ts:getSiteConfig()`, `lib/services/booking.service.ts`, `lib/services/payment.service.ts`, `lib/services/availability.service.ts` | `app/(site)/page.tsx`, `app/(site)/HomeClient.tsx`, `app/(site)/booking/page.tsx`, `app/(site)/rooms/page.tsx` | Complete |
| 119 | Accurate Price Resolution on Booking Page | Booking page calls `/api/v1/pricing/resolve` API when dates change to get server-resolved pricing (applying weekend, seasonal, festival, and long-stay rules). Eliminates mismatch between displayed estimate and actual Razorpay charge. | `app/api/v1/pricing/resolve/route.ts`, `lib/services/pricing.service.ts:resolvePrice()` | `app/(site)/booking/page.tsx` — `useEffect` on date/roomType change fetches resolved total | Complete |

---

## Module 17: File Upload

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 118 | Single Image Upload | Validates file type (jpg/png/webp) and size (5MB max). Supports local storage (public/uploads/) and configurable cloud storage. Returns URL + filename. | `app/api/v1/upload/image/route.ts` (POST), `lib/storage/index.ts:uploadFile()` | Used in admin room type image upload | Complete |
| 119 | Multiple Images Upload | Uploads multiple images (up to 10). | `app/api/v1/upload/images/route.ts` (POST) | Used in admin room type image management | Complete |
| 120 | Delete Uploaded File | Deletes file from storage. | `app/api/v1/upload/[fileId]/route.ts` (DELETE), `lib/storage/index.ts:deleteFile()` | Used in admin when removing room images | Complete |

---

## Module 18: Health & Monitoring

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 121 | Public Health Check | Returns `{status: "ok", timestamp, version, uptime}`. No auth required. | `app/api/v1/health/route.ts` (GET) | N/A | BE Only |
| 122 | Detailed Health Check | Admin only. Checks database (raw query), Redis (ping), storage connectivity. Returns per-service status (ok/error/not_configured) and overall status (ok/degraded/down). | `app/api/v1/health/detailed/route.ts` (GET) | N/A | BE Only |

---

## Module 19: Contact

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 123 | Contact Form | Accepts name, email, subject, message. | `app/api/v1/contact/route.ts` (POST) | `app/(site)/contact/page.tsx` — form with name, email, subject, message fields. Shows hotel address, phone, email, hours. | Complete |

---

## Module 20: Site Pages (FE Only)

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 124 | Home Page | Hero with parallax, pricing bar, highlights section, sticky scroll experience, gallery preview, rooms preview, reviews, CTAs. SSR fetches room types + reviews. | SSR uses `listRoomTypes()`, `listPublicReviews()` | `app/(site)/page.tsx`, `app/(site)/HomeClient.tsx` | Complete |
| 125 | About Page | Static content page about the villa and founder. | N/A | `app/(site)/about/page.tsx` | FE Only |
| 126 | Gallery Page | Static image gallery with category filtering, masonry layout, lightbox viewer with keyboard navigation. | N/A | `app/(site)/gallery/page.tsx` | FE Only |
| 127 | Room Detail Page | Dynamic page showing room type details, images, amenities, description. | SSR uses `getRoomType()` | `app/(site)/rooms/[id]/page.tsx`, `app/(site)/rooms/[id]/RoomDetailClient.tsx` | Complete |
| 128 | Reviews Page | Public reviews listing (SSR). Shows all approved reviews with ratings and user names. | SSR uses `listPublicReviews()` | `app/(site)/reviews/page.tsx` | Complete |
| 129 | Cancellation Policy Page | Static content page with cancellation policy text. | N/A | `app/(site)/cancellation/page.tsx` | FE Only |
| 130 | Privacy Policy Page | Static content page. | N/A | `app/(site)/privacy/page.tsx` | FE Only |
| 131 | Terms of Service Page | Static content page. | N/A | `app/(site)/terms/page.tsx` | FE Only |
| 132 | Refund Policy Page | Static content page. | N/A | `app/(site)/refund/page.tsx` | FE Only |
| 133 | 404 Not Found Page | Custom not found page. | N/A | `app/not-found.tsx` | FE Only |
| 134 | Booking Search Component | Date range + guests picker on home hero. Links to `/booking?checkIn=&checkOut=&guests=`. | N/A | `app/(site)/components/BookingSearch.tsx` | FE Only |
| 135 | Date Range Picker | Custom calendar component with blocked date visualization. | N/A | `app/(site)/components/DateRangePicker.tsx` | FE Only |
| 136 | Navbar | Site-wide navigation with logo, links (Rooms, Gallery, Reviews, About, Contact), auth state (Login/Account). | N/A | `app/(site)/components/Navbar.tsx` | FE Only |
| 137 | Footer | Site-wide footer with navigation links, contact info, social links. | N/A | `app/(site)/components/Footer.tsx` | FE Only |
| 138 | WhatsApp Float | Floating WhatsApp button for quick contact. | N/A | `app/(site)/components/WhatsAppFloat.tsx` | FE Only |
| 139 | Animated Sections | Reusable animation components: FadeIn, BlurFadeIn, StaggerContainer, StickyScrollSection, CinematicSection, AnimatedCounter, RevealOnScroll, PageHero, ScrollScaleImage. Uses framer-motion. | N/A | `app/(site)/components/AnimatedSection.tsx` | FE Only |

---

## Module 21: Admin Panel (FE Infrastructure)

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 140 | Admin Auth Guard | Validates admin token on every page load. Checks role is admin/super_admin. Redirects to /admin/login if unauthorized. | N/A | `app/admin/components/AuthGuard.tsx` | FE Only |
| 141 | Admin Login Page | Admin-specific login form. Stores token as `localStorage("token")` (separate from site token). | N/A | `app/admin/login/page.tsx` | FE Only |
| 142 | Admin Sidebar Navigation | 14-item navigation: Dashboard, Bookings, Availability, Rooms, Pricing, Amenities, Coupons, Users, Payments, Reviews, Notifications, Reports, Audit Logs, Settings. Collapsible on mobile. | N/A | `app/admin/components/Sidebar.tsx` | FE Only |
| 143 | Admin Shell | Layout wrapper with Sidebar + AuthGuard + Toast provider. | N/A | `app/admin/components/AdminShell.tsx` | FE Only |
| 144 | Admin API Helper | `get()`, `post()`, `patch()`, `del()`, `uploadFormData()` functions with auto Bearer token from `localStorage("token")`. | N/A | `lib/admin/api.ts` | FE Only |
| 145 | useApi / useApiList Hooks | Custom hooks for data fetching with loading/error states and pagination metadata. | N/A | `app/admin/hooks/useApi.ts` | FE Only |
| 146 | DataTable Component | Reusable table with columns, sorting, loading state. | N/A | `app/admin/components/DataTable.tsx` | FE Only |
| 147 | Confirm Modal | Reusable confirmation dialog for destructive actions. | N/A | `app/admin/components/ConfirmModal.tsx` | FE Only |
| 148 | Modal Component | Generic reusable modal. | N/A | `app/admin/components/Modal.tsx` | FE Only |
| 149 | Toast Notifications | Toast notification system for admin UI. | N/A | `app/admin/components/Toast.tsx` | FE Only |
| 150 | Badge Component | Status badge with color mapping. | N/A | `app/admin/components/Badge.tsx` | FE Only |
| 151 | StatsCard Component | Dashboard stat card. | N/A | `app/admin/components/StatsCard.tsx` | FE Only |
| 152 | Skeleton Loaders | Loading skeleton components for cards and charts. | N/A | `app/admin/components/Skeleton.tsx` | FE Only |
| 153 | FormField Components | Reusable form field components (Input, Select, etc.). | N/A | `app/admin/components/FormField.tsx` | FE Only |
| 154 | PageHeader Component | Reusable page header with title and description. | N/A | `app/admin/components/PageHeader.tsx` | FE Only |
| 155 | Admin Redirect | `/admin` root redirects to `/admin/dashboard`. | N/A | `app/admin/page.tsx` | FE Only |

---

## Module 22: Infrastructure & Middleware

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 156 | Centralized Error Handler | `withErrorHandler()` wraps all route handlers. Catches known errors (BadRequest, Unauthorized, Forbidden, NotFound, Conflict, TooManyRequests) and returns standardized JSON responses with appropriate HTTP status codes. | `lib/middleware/with-error-handler.ts` | N/A | BE Only |
| 157 | Standardized API Response | `successResponse(data, message, status)` and `paginatedResponse(data, total, page, limit)` helpers for consistent response shape. | `lib/utils/api-response.ts` | N/A | BE Only |
| 158 | Custom Error Classes | BadRequestError (400), UnauthorizedError (401), ForbiddenError (403), NotFoundError (404), ConflictError (409), TooManyRequestsError (429). | `lib/utils/errors.ts` | N/A | BE Only |
| 159 | Zod Validation Middleware | `validateBody(req, schema)` and `validateQuery(req, schema)` middleware for request validation. | `lib/middleware/validate.ts`, `lib/validators/*.ts` | N/A | BE Only |
| 160 | Rate Limiting | In-memory rate limiter with tiered configs: auth (10 req/15min), api (100 req/min), upload (20 req/min). Per-IP tracking. | `lib/middleware/rate-limit.ts` | N/A | BE Only |
| 161 | JWT Utilities | Sign and verify tokens using jose library. Separate secrets for access (15min) and refresh (7d) tokens. | `lib/utils/jwt.ts` | N/A | BE Only |
| 162 | Password Hashing | bcrypt with salt rounds. Hash and compare functions. | `lib/utils/password.ts` | N/A | BE Only |
| 163 | Token Generation | `generateToken()` (32-byte random hex) and `generateBookingReference()` (VEL-XXXXXX format). | `lib/utils/tokens.ts` | N/A | BE Only |
| 164 | Database Client | Prisma client singleton (prevents hot-reload connection leaks). | `lib/db.ts` | N/A | BE Only |
| 165 | Redis Client | Optional Redis connection using ioredis. Gracefully handles connection errors. | `lib/redis.ts` | N/A | BE Only |
| 166 | Environment Validation | Zod schema validates required env vars at import time (fail-fast). | `lib/env.ts` | N/A | BE Only |
| 167 | Logger | Structured logger utility. | `lib/utils/logger.ts` | N/A | BE Only |
| 168 | Request Parameter Parsing | `getSearchParams()` helper for extracting query parameters. | `lib/utils/params.ts` | N/A | BE Only |
| 169 | Email Sending | Email service with Resend integration. Sends verification emails on registration, password reset emails on forgot-password, and resend verification from account page. | `lib/email/send.ts`, `lib/email/helpers.ts` | N/A | BE Only |
| 170 | Background Jobs | Job queue with BullMQ/Redis for async processing. Processors for booking confirmation, payment receipt, booking reminders, expired hold release, pending payment verification. | `lib/jobs/queue.ts`, `lib/jobs/processors.ts`, `lib/jobs/init.ts` | N/A | BE Only |

---

## Module 23: SEO & DevOps

| # | Feature Name | Description | BE Evidence | FE Evidence | Status |
|---|---|---|---|---|---|
| 171 | SEO Metadata | `buildPageMetadata()` helper for per-page Open Graph, Twitter, and canonical URL metadata. | `lib/seo.ts` | Used in all layout.tsx files | Complete |
| 172 | Sitemap | Dynamic sitemap.xml generation. | `app/sitemap.ts` | N/A | Complete |
| 173 | Robots.txt | Dynamic robots.txt generation. | `app/robots.ts` | N/A | Complete |
| 174 | API Documentation | Swagger/OpenAPI spec endpoint. | `app/api/docs/route.ts` (JSON spec), `app/api/docs/ui/route.ts` (UI) | N/A | BE Only |
| 175 | Docker Support | Dockerfile with multi-stage build. docker-compose with app + PostgreSQL + Redis services. | `Dockerfile`, `docker-compose.yml` | N/A | BE Only |
| 176 | CI/CD Pipeline | GitHub Actions: lint, type-check, test, build on PR/push to main. | `.github/workflows/ci.yml` | N/A | BE Only |
| 177 | Database Seeding | Seed script for initial data (room types, room units, amenities, admin user, sample bookings). | `prisma/seed.ts` | N/A | BE Only |
| 178 | Unit Tests | Jest tests for utilities: api-response, errors, jwt, password, tokens, rate-limit middleware. | `tests/unit/**/*.test.ts`, `tests/setup.ts`, `jest.config.ts` | N/A | BE Only |

---

## Previously Unresolved — Now Resolved

All 11 previously flagged items have been addressed:

1. **Logout** (#9) — **Resolved**: FE now calls `POST /api/v1/auth/logout` with refresh token before clearing localStorage.
2. **Guest Token** (#11) — **Resolved**: FE booking page now calls `POST /api/v1/auth/guest-token` instead of `crypto.randomUUID()`.
3. **Coupon Application** (#81, #82) — **Resolved**: Booking page has coupon input field with validate/apply flow. `createBooking()` validates and applies coupons. `roomTypeId` made optional in validator.
4. **Multi-Room Booking** (#62) — **Removed**: `isMultiRoom` removed from Prisma schema, booking validator, and booking service. Single-villa property.
5. **Refresh Token** (#8) — **Resolved**: `apiFetch()` in useAuth intercepts 401, calls `tryRefreshToken()`, retries original request.
6. **Email Sending** (#169) — **Resolved**: `register()` sends verification email, `forgotPassword()` sends reset email via Resend. Resend verification endpoint added.
7. **Room Type Breakdown** (#103) — **Resolved**: PieChart added to admin dashboard showing booking distribution by room type.
8. **Audit Logs FE** (#113, #114) — **Resolved**: Admin audit logs page created with search/filter/pagination. Added to sidebar navigation.
9. **Search Feature** (#90) — **Removed**: `search.service.ts` and `GET /api/v1/search/rooms` route deleted. Not needed for single-villa property.
10. **Review Submission** (#91) — **Resolved**: "Write a Review" form added to account reviews page with booking selector, star rating, title, body.
11. **Reset Password Page** (#5) — **Resolved**: `app/(site)/auth/reset-password/page.tsx` created with token from URL, password + confirm fields.

**Additional change**: Users can now login without verified email. Verification notice with resend button shown on account page.

---

## Summary

| Metric | Count |
|---|---|
| **Total Modules** | 22 (Module 10 Search removed) |
| **Total Features** | 178 (2 removed: multi-room booking, search) |
| **Complete (BE + FE)** | 120 |
| **BE Only** | 25 |
| **FE Only** | 31 |
| **Removed** | 2 |
| **Needs Clarification** | 0 |
